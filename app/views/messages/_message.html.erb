<div id="<%= dom_id message %>">
  <p>
    <strong>Body:</strong>
    <%= message.body %>
  </p>
  <p>
    <%= link_to "Edit this message", edit_message_path(message), remote: true, format: :turbo_stream %>
    <%= button_to "Destroy this message", message, method: :delete %>
  </p>
</div>
